apiVersion: v1
kind: ConfigMap
metadata:
  name: orchestrator-config
  labels:
    app: orchestrator-ai
data:
  LOG_LEVEL: "INFO"
  LOG_FORMAT: "json"
  ENABLE_METRICS: "true"
  METRICS_PORT: "9090"
  ENVIRONMENT: "production"
  REDIS_HOST: "orchestrator-redis"
  REDIS_PORT: "6379"

